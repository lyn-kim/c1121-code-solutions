/* exported getFirstChar */

// storage for first letter
// take first letter out from the string and store it to storage
// give back the letter

function getFirstChar(string) {
  var character = string[0];
  return character;
}
