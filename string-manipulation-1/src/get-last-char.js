/* exported getLastChar */

// create storage for last character
// take out last character from string and put into storage
// give back last character

function getLastChar(string) {
  var character = string[string.length - 1];
  return character;
}
