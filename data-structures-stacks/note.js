// balanced braces

// define a function that takes a string including parentheses, brackets, braces
// return true/false if the braces are balanced
// '{({[]{([])}})}' --> true
// '{({[]{([)]}})}' -- > false

// browser history is a stack
